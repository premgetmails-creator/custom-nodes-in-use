[project]
name = "comfyui-ltx-visual-prompt-memory"
version = "0.1.0"
description = "Frozen multi-reference visual memory nodes for LTX video in ComfyUI"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
